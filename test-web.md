📋 برومبت احترافي لإنشاء موقع استقدام خادمات متكامل — مشروع "test-web"
كيف تستخدم هذا الملف؟
انسخ كل المحتوى من بداية القسم ## 🎯 الطلب حتى نهاية الملف، ثم ألصقه بالكامل في محادثة مع وكيل ذكاء اصطناعي برمجي (مثل OpenHands أو أي agent مهندس كود) ليُنشئ لك موقعاً متكاملاً خطوة بخطوة.

هذا البرومبت مبني على فحص فعلي كامل لموقع استقدام خادمات قطري مرجعي (rozana-manpower.com): كل الصفحات، الأزرار، النماذج، مسارات التنقّل، وآلية "الحجز" التي تنتهي بنموذج استفسار/واتساب (لا توجد بوابة دفع إلكتروني).

الاسم test-web هو اسم كودي مؤقت — صُمّم المشروع بحيث يُستبدل من مكان واحد فقط لاحقاً.

🎯 الطلب
أريدك أن تبني لي موقعاً إلكترونياً متكاملاً لشركة استقدام عمالة منزلية (خادمات/مربيات/عاملات منازل) في قطر. اسم المشروع الكودي حالياً هو test-web (سيُستبدل لاحقاً بالاسم التجاري الفعلي عند الإطلاق، لذا صمّم كل النصوص والمسارات بحيث يكون استبدال الاسم سهلاً عبر متغيّر/ثابت مركزي واحد).

موقع الإلهام المرجعي (للفهم فقط، لا تنسخ كوداً منه): موقع استقدام خادمات قطري. استخلصتُ لك تحليلاً كاملاً لبنته ومساراته في الأسفل — نفّذه بدقة واحترافية.

🧱 التقنيات المطلوبة
Frontend: Next.js (App Router, TypeScript) مع React Server Components حيث يناسب.
Styling: TailwindCSS + CSS Modules عند الحاجة. تصميم RTL بالكامل (عربي رئيسي) مع دعم LTR (إنجليزي) عبر [locale] أو app/[lang]/.
Backend/API: Next.js Route Handlers (أو Node.js + Express منفصل إذا تفضّل، لكن Next.js API routes أفضل للتقليل من التعقيد).
قاعدة بيانات: ابدأ بـ SQLite (Prisma) للمرحلة التجريبية، مع تصميم schema جاهز للترحيل إلى PostgreSQL لاحقاً.
i18n: دعم لغتين (عربي ar افتراضي + إنجليزي en) عبر ملفات JSON للترجمات + مبدّل لغة في الهيدر.
بدون نظام تسجيل دخول للعملاء في هذه المرحلة — فقط لوحة تحكم admin بسيطة لاحقاً (اختياري).
بدون بوابة دفع — كل الحجز ينتهي بنموذج استفسار أو واتساب (كما في المرجع).
🎨 الهوية البصرية والتصميم
لوحة الألوان (مقترحة، قابلة للتعديل من ملف ثابت واحد):
اللون الأساسي (Primary): ذهبي/كهرماني دافئ #C8A04A (يوحي بالثقة والفخامة الخليجية)
لون مماثل (Secondary): أزرق داكن #1B3A5C
خلفية: أبيض ناصع #FFFFFF + رمادي فاتح جداً #F8FAFC
نص: رمادي قاتم #1F2937
ألوان الحالة: أخضر #16A34A (متاحة)، ذهبي #F59E0B (موثّقة)
اجعل الألوان في tailwind.config.ts كـ tokens مسماة لتسهيل إعادة الاسم التجاري لاحقاً.
الخطوط:
العربي: "Cairo" أو "Tajawal" (Google Fonts)
الإنجليزي: "Inter"
تحميل عبر next/font.
الحالة العامة:
تصميم نظيف، فاخر، يبعث الثقة، اتجاه RTL.
حركات لطيفة (Framer Motion أو CSS transitions): fade-in عند الظهور، عدّادات أرقام متحركة، hover على البطاقات.
زر عائم ثابت أسفل الشاشة (واتساب + اتصال + الموقع على الخريطة).
متجاوب بالكامل (Mobile-first): جوال، تابلت، ديسكتوب.
🗺️ خريطة الموقع (Sitemap) — نفّذ كل صفحة
/                       → الصفحة الرئيسية
/candidates             → كتالوج الخادمات (فلترة/بحث/ترتيب/تصفّح)
/maid/[slug]            → صفحة خادمة فردية (بيانات + سيرة + نموذج حجز)
/services               → الخدمات (قائمة + روابط لصفحات فرعية)
/services/full-time-live-in-maids
/services/housemaid-services
/services/recruitment-deployment
/services/maids-training
/maids/filipina-maids    → صفحة جنسية (تُفلتر الكتالوج)
/maids/ethiopian-maids
/maids/ugandan-maid
/maids/nanny-in-qatar
/about-us               → من نحن
/favourite              → المفضلة (محفوظات القلب)
/contact-us             → تواصل معنا (نموذج + خريطة)
/blogs                  → المدونة (قائمة مقالات)
/post/[slug]           → مقال فردي
/terms-and-conditions   → الشروط والأحكام
/privacy-policy         → سياسة الخصوصية
/lang/en                → تبديل اللغة (أو /en/ كـ prefix)
/admin                  → (اختياري، لاحقاً) لوحة إدارة المرشحات
/api/candidates         → API فلترة/بحث الكتالوج (JSON)
/api/inquiry            → استقبال نماذج الاستفسار/الحجز
/api/favorites/toggle   → تبديل المفضلة
/api/newsletter         → (اختياري) النشرة البريدية
📄 مواصفات كل صفحة بالتفصيل
1) الصفحة الرئيسية (/)
الأقسام من الأعلى للأسفل:

Top bar: علم قطر + نص "مكتب استقدام مرخّص ومعتمد لخدمة جميع مناطق قطر" (يمين) + رقم الهاتف (رابط tel:) + زر تبديل اللغة (يسار).
Header/Sticky: شعار test-web (متغيّر) + قائمة تنقّل: الرئيسية / الخادمات المتاحات / خدماتنا / من نحن / المفضلة / تواصل معنا. يصبح شفافاً ثم يأخذ ظلاً عند التمرير. زر بارز "تصفّح الخادمات".
Hero: عنوان كبير "رحّب بخادمة موثوقة تناسب منزلك في قطر" (مع كلمة "خادمة موثوقة" بلون مميز) + فقرة وصف + زرّان:
أساسي: "تصفّح الخادمات المتاحات" → /candidates
ثانوي: "احصل على عرض مجاني" → تمرير ناعم لنموذج #contact أسفل الصفحة.
شارات ثقة صغيرة: "ملفات موثّقة 100%" / "مرخّص في قطر" / "توظيف سريع".
شريط متحرك (ticker) بنوعيات الخدمة: خادمات فلبينيات / إثيوبيات / أوغنديات / إقامة كاملة / مربيات ورعاية مسنين / طبخ وتدبير منزلي.
إحصائيات (عدّادات متحركة عند الظهور): عدد العائلات المخدومة، عدد الخادمات المتاحة، سنوات الخبرة، متوسط التقييم (مثل 4.9/5).
"لماذا تختار [الشركة]": 5-6 بطاقات مزايا (أيقونة + عنوان + وصف): مرخّص ومعتمد / خادمات مدرّبات وموثّقات / إجراءات شفّافة / دعم مستمر / إقامة كاملة / جنسيات متعددة.
"خادمات متاحات مختارة": 6 بطاقات مرشحات (تُجلب من DB) + زر "عرض جميع الخادمات +N" → /candidates.
"وظّف في 3 خطوات": 1) اختر خادمتك 2) وافق وأكمل المستندات 3) رحّب بها في منزلك — أرقام كبيرة + أيقونات.
قسم الأسعار: "تبدأ من X ر.ق / شهرياً" + مزايا (عروض شاملة / ملفات موثّقة / ضمان استبدال) + زر "اطلب عرضك المجاني" + زر "شاهد الملفات والأسعار".
شهادات عائلات: 3 بطاقات تقييم (نجوم + اقتباس + اسم + منطقة + أحرف أولى).
"أدلّة للعائلات": 3 أحدث مقالات من المدونة + روابط "اقرأ المزيد".
الأسئلة الشائعة: Accordion (5+ أسئلة) — كل عنصر يفتح/يغلق.
نموذج "اطلب عرضاً مجانياً" (id="contact"): حقول: الاسم الكامل، الهاتف/واتساب، البريد الإلكتروني، نوع الخدمة (select: خادمة إقامة كاملة / مربية / طبخ / رعاية مسنين)، الرسالة. زر "إرسال الطلب" → POST /api/inquiry. عند النجاح: رسالة toast/flash خضراء "تم استلام طلبك، سنتواصل معك قريباً" + إفراغ الحقول.
معلومات التواصل: هاتف، إيميل، العنوان، ساعات العمل + أيقونات.
Footer: 4 أعمدة:
الشركة: الرئيسية/خدماتنا/من نحن/المدونة/تواصل/الشروط/الخصوصية
عاملاتنا حسب الجنسية: 4 روابط
ابقَ على تواصل: نموذج نشرة بريدية (بريد + زر اشترك)
سوشيال: فيسبوك، إنستغرام، لينكدإن، تيك توك، واتساب (أيقونات)
أسفل: © السنة الحالية [اسم الشركة]. جميع الحقوق محفوظة.
2) صفحة الخادمات المتاحات (/candidates)
شريط فلاتر (chips قابلة للنقر، تفعيل/إلغاء):
الجنسية: فلبينية / إثيوبية / أوغندية
الخبرة: 0-2 سنة / 3-5 سنوات / +6 سنوات
اللغة: الإنجليزية / العربية
الدين: مسيحية / مسلمة
زر "إعادة تعيين الكل"
بحث نصي (placeholder: "ابحث بالاسم أو المهارة…") + debounce 300ms.
ترتيب (select): الموصى به / الراتب من الأقل للأعلى / من الأعلى للأقل / الأكثر خبرة / الاسم أ-ي.
مبدّل عرض: شبكة (grid) / قائمة (list) — يُحفظ في URL ?view=grid|list.
الشبكة: بطاقات مرشحات. كل بطاقة:
صورة (مع شارة "متاحة" خضراء + "موثّقة" ذهبية)
اسم المرشحة (كبير) + جنسية + دين
شبكة بيانات صغيرة: الخبرة، اللغات، الحالة الاجتماعية، عملت في
الراتب (مثل 1,200 ر.ق / شهرياً)
زر "عرض الملف" → /maid/[slug]
زر "وظّفها الآن" → واتساب برسالة جاهزة
أيقونة قلب (حفظ في المفضلة) في الزاوية
تحميل لAZY: زر "تحميل المزيد" يستدعي /api/candidates?page=N&... ويعيد JSON { html, total, hasMore } أو بيانات منسقة. تحديث العدد و"لا نتائج" عند الانتهاء.
كل الفلاتر/البحث/الترتيب تُحدّث الشبكة فوراً عبر AJAX بدون إعادة تحميل كاملة، وتُحدّث URL query params (للمشاركة المباشرة).
زر مساعد علوي "هل تحتاج لمساعدة؟" → نموذج #contact.
3) صفحة الخادمة الفردية (/maid/[slug])
مسار التنقّل (breadcrumb): الرئيسية / الخادمات المتاحات / [اسم المرشحة]
بطاقة العنوان: اسم المرشحة + رقم مرجعي (مثل outside-1747) + شارات "متاحة حالياً" و"موثّقة".
صورة كبيرة للمرشحة.
3 أزرار رئيسية:
وظّفها الآن (واتساب) — رابط https://wa.me/[رقم المكتب]?text=مرحباً، أنا مهتم بالخادمة: [name] ([uid])
تحميل السيرة الذاتية (PDF) — رابط لصفحة/ملف السيرة (يمكن توليد PDF أو عرض صفحة CV).
حفظ في المفضلة (قلب) — POST /api/favorites/toggle/[uid]؛ النص يتبدّل لـ"محفوظ في المفضلة"؛ القلب يصبح ممتلئاً.
جدول "البيانات الشخصية والمهارية": الجنسية، الدين، الحالة الاجتماعية، عدد الأطفال، سنوات الخبرة، اللغات المتحدثة، البلدان التي عملت بها، الراتب الشهري المتوقع.
نموذج "طلب حجز واستعلام سريع": حقول: الاسم الكامل (مطلوب)، رقم الهاتف/واتساب (مطلوب)، ملاحظات إضافية (textarea، مُعبّأة مسبقاً بنص "استفسار بخصوص المرشحة: [name] ([uid])"). زر "إرسال طلب الاستعلام" → POST /api/inquiry مع candidate_uid. عند النجاح: toast + إفراغ + بقاء على الصفحة.
"مرشحات أخريات متاحات": 3 بطاقات مقترحة + زر "عرض المزيد من المرشحات" → /candidates.
4) صفحة الخدمات (/services) + الصفحات الفرعية
صفحة عامة تشرح: توظيف الخادمات والمربيات / خدمات العمالة المنزلية / تدريب العاملات / خادمات مقيمات بدوام كامل.
لكل خدمة فرعية صفحة مستقلة بعنوان (H1) ومحتوى تشرحي + صورة + قائمة مزايا + FAQ مخصص + زر CTA "اطلب عرضك المجاني".
روابط داخلية بينها وبين الكتالوج.
5) صفحات الجنسيات (/maids/[nationality])
عنوان (H1) مثل "خادمات فلبينيات في قطر".
فقرة SEO + بطاقات مرشحات من هذه الجنسية (تُجلب من /api/candidates?nationality=X أو توجّه لـ /candidates?nationality=X).
روابط للجنسيات الأخرى.
6) صفحة المفضلة (/favourite)
عنوان "الخادمات المحفوظة".
إن توجد مفضلات: شبكة بطاقات (نفس تصميم بطاقة الكتالوج) + زر إزالة (قلب ممتلئ → إزالة → إعادة تحميل).
إن فارغة: أيقونة + "لا توجد خادمات محفوظة" + نص توضيحي + زر "تصفّح الخادمات".
7) صفحة تواصل معنا (/contact-us)
نموذج طلب: الاسم، الهاتف، البريد، نوع الخدمة (select)، الرسالة → POST /api/inquiry.
خريطة Google Maps مدمجة (iframe) لموقع المكتب.
بطاقات معلومات: هاتف، إيميل، العنوان، ساعات العمل.
أيقونات سوشيال.
8) صفحة "من نحن" (/about-us)
نبذة المكتب + الرسالة + الهدف + الرؤية والقيم (3 مبادئ) + لماذا تثق العائلات + الخدمات المتخصصة + الالتزام بالتدريب + عملية التوظيف السلسة + FAQ.
9) المدونة (/blogs + /post/[slug])
/blogs: شبكة مقالات (صورة + عنوان + مقتطف + تصنيف + تاريخ + رابط "اقرأ المزيد").
/post/[slug]: مقال كامل (H1 + محتوى + صور) + مقالات ذات صلة.
10) الشروط والخصوصية
صفحات نصية قانونية بمحتوى نموذجي قابل للتعديل لاحقاً.
⚙️ الوظائف التفاعلية (Features) المطلوبة
أ) كتالوج المرشحات (Candidates)
تخزين البيانات في جدول Candidate بالحقول: id, uid (slug-friendly, unique), slug, name, nationality, religion, marital_status, children_count, experience_years, languages (array), countries_worked (array), salary, currency, status (available/booked), verified (bool), photo_url, cv_url, skills, video_url?, created_at.
API /api/candidates يقبل: page, view, nationality, religion, language, experience, q (search), sort ويعيد JSON { data: [...], total, hasMore, html (optional) }.
الفلاتر تُحدّث URL (قابلة للمشاركة) وتُسترجع عند فتح الرابط.
ب) المفضلة (Favorites)
بدون تسجيل دخول: خزّن في localStorage مصفوفة favorite_uids.
زر القلب يبدّل الحالة محلياً + يحدّث صفحة /favourite.
(اختياري لاحقاً) POST /api/favorites/toggle/[uid] للمزامنة الخادمية.
ج) نماذج الاستفسار (Inquiry)
API /api/inquiry (POST) يستقبل: name, phone, email?, message, candidate_uid?, looking_for?, source_page?.
يحفظ في جدول Inquiry + يرسل إشعاراً (بريد إلكتروني للمكتب + اختياري webhook واتساب).
التحقق من الحقول (validation) + CSRF/honeypot مكافحة السبام.
استجابة: redirect (للنماذج التقليدية) أو JSON { success: true, message } (للأجاكس) + رسالة toast.
د) النشرة البريدية
نموذج بريد + زر اشترك → POST /api/newsletter (احفظ في جدول Subscriber + تحقق صحة البريد). (يمكن البدء بنسخة بسيطة).
هـ) زر التواصل العائم (Floating Actions)
ثابت أسفل اليمين: واتساب (wa.me)، اتصال (tel:)، الموقع (Google Maps) — يظهر في كل الصفحات.
و) i18n (تعدد اللغات)
ملفات messages/ar.json و messages/en.json.
كل النصوص عبر دالة t("key") — لا تثبّت أي نص عربي مباشرة في المكونات.
مبدّل لغة في الهيدر يحفظ الاختيار في cookie ويعيد توجيه لنظير الصفحة باللغة الأخرى.
ز) SEO
لكل صفحة: metadata (title, description, openGraph, twitter).
sitemap.xml + robots.txt ديناميكيان.
روابط canonical + hreflang (ar/en).
بيانات منظمة (JSON-LD): LocalBusiness للمكتب + Person لكل خادمة + Article للمدونة.
عناوين محسّنة (مثل "خادمة فلبينية في قطر | استقدام عاملات فلبينيات | [الشركة]").
ح) الأداء والجودة
next/image للصور + lazy loading + تنسيق WebP/AVIF.
مكونات الخادم (RSC) افتراضياً، العميل ("use client") فقط عند الحاجة (الفلاتر، المفضلة، النماذج، الأكورديون).
كود نظيف، مكوّنات معاد استخدامها (Button, Card, Input, Modal, Toast, Breadcrumb, Section).
إخفاء اسم الشركة خلف ثابت واحد (مثل SITE_CONFIG.name = "test-web") — كل المراجع تأتي منه لتسهيل إعادة التسمية لاحقاً.
🧩 ملف الإعدادات المركزي (مطلوب)
أنشئ ملف src/config/site.ts يحتوي كل القيم القابلة للتبديل لاحقاً في مكان واحد:

export const SITE = {
  name: "test-web",            // ← يُستبدل بالاسم التجاري لاحقاً
  nameAr: "تست-ويب",            // الاسم العربي
  taglineAr: "مكتب استقدام خدم مرخّص في قطر",
  phone: "+97444537000",
  whatsapp: "97444537000",
  email: "info@test-web.com",
  addressAr: "مكتب رقم 012، مبنى 328، شارع 280، معيذر 55، الريان، قطر",
  workingHoursAr: "السبت–الخميس · 9ص – 9م",
  mapEmbedUrl: "https://maps.google.com/...",
  social: {
    facebook: "...", instagram: "...", linkedin: "...",
    tiktok: "...", whatsapp: "https://wa.me/97444537000",
  },
  minSalary: 1400,
  currency: "ر.ق",
  defaultLocale: "ar",
  locales: ["ar", "en"],
} as const;
كل المكوّنات تستورد من هذا الملف — عند إطلاق المشروع يُغيَّر مكان واحد فقط.

🌱 البيانات الأولية (Seed Data)
زوّد المشروع بـ:

8-12 مرشحة نموذجية (جنسيات متنوعة: فلبينية/إثيوبية/أوغندية، رواتب 1200-1800، خبرات مختلفة، لغات، أديان، حالات اجتماعية) مع صور placeholder (استخدم https://i.pravatar.cc أو placehold.co).
4 مقالات مدونة نموذجية حول استقدام الخادمات.
3 شهادات عائلات.
5 أسئلة شائعة.
4 صفحات خدمات.
4 صفحات جنسيات.
📦 هيكل المشروع المقترح
test-web/
├─ src/
│  ├─ app/
│  │  ├─ [locale]/
│  │  │  ├─ page.tsx                 (الرئيسية)
│  │  │  ├─ candidates/page.tsx
│  │  │  ├─ maid/[slug]/page.tsx
│  │  │  ├─ services/page.tsx
│  │  │  ├─ services/[slug]/page.tsx
│  │  │  ├─ maids/[nationality]/page.tsx
│  │  │  ├─ about-us/page.tsx
│  │  │  ├─ favourite/page.tsx
│  │  │  ├─ contact-us/page.tsx
│  │  │  ├─ blogs/page.tsx
│  │  │  ├─ post/[slug]/page.tsx
│  │  │  ├─ terms-and-conditions/page.tsx
│  │  │  └─ privacy-policy/page.tsx
│  │  ├─ api/
│  │  │  ├─ candidates/route.ts
│  │  │  ├─ inquiry/route.ts
│  │  │  ├─ favorites/toggle/[uid]/route.ts
│  │  │  └─ newsletter/route.ts
│  │  ├─ layout.tsx
│  │  └─ globals.css
│  ├─ components/
│  │  ├─ layout/ (Header, Footer, TopBar, FloatingActions, Breadcrumb)
│  │  ├─ home/ (Hero, Stats, WhyUs, FeaturedMaids, Steps, Pricing, Testimonials, BlogTeaser, FAQ, ContactForm)
│  │  ├─ candidates/ (Filters, SearchBar, SortSelect, ViewToggle, MaidCard, LoadMore, NoResults)
│  │  ├─ maid/ (MaidHeader, MaidDetails, MaidActions, InquiryForm, RelatedMaids)
│  │  ├─ ui/ (Button, Input, Select, Textarea, Badge, Toast, Accordion, Card, Modal)
│  │  └─ i18n/ (LanguageSwitcher)
│  ├─ lib/
│  │  ├─ prisma.ts
│  │  ├─ validators.ts (zod)
│  │  ├─ whatsapp.ts
│  │  └─ utils.ts
│  ├─ messages/
│  │  ├─ ar.json
│  │  └─ en.json
│  ├─ config/
│  │  └─ site.ts
│  └─ hooks/
│     └─ useFavorites.ts
├─ prisma/
│  ├─ schema.prisma
│  └─ seed.ts
├─ public/ (images, icons, robots.txt)
├─ tailwind.config.ts
├─ next.config.mjs
├─ tsconfig.json
└─ package.json
✅ قائمة التحقق النهائية (Checklist) — تأكد أن الوكيل ينفّذها كلها
 كل الصفحات الـ 12+ تعمل وتُحمّل بدون أخطاء.
 كتالوج الخادمات: فلترة + بحث + ترتيب + تحميل أكثر + مبدّل عرض، كلها أجاكس وتُحدّث URL.
 صفحة خادمة: بيانات + 3 أزرار (واتساب/PDF/مفضلة) + نموذج حزب → toast نجاح.
 المفضلة تعمل عبر localStorage وتظهر في /favourite.
 نماذج الاستفسار: تحقق + حفظ في DB + رسالة نجاح/خطأ.
 دعم لغتين (ar/en) كامل + مبدّل لغة يحفظ الاختيار.
 تصميم RTL + متجاوب (جوال/تابلت/ديسكتوب).
 زر عائم (واتساب/اتصال/خريطة) في كل الصفحات.
 SEO: metadata + sitemap + robots + JSON-LD + hreflang.
 اسم الشركة test-web معزول في src/config/site.ts (قابل للتبديل بمكان واحد).
 بيانات seed جاهزة (مرشحات + مقالات + شهادات + FAQ).
 كود نظيف، مكوّنات معاد استخدامها، بدون تكرار.
 تعليمات تشغيل واضحة في README: npm install, npx prisma db push, npm run seed, npm run dev.
🚀 تعليمات التشغيل النهائية
في النهاية أعطني:

أوامر التشغيل خطوة بخطوة.
كيف أغيّر اسم الشركة لاحقاً (مكان واحد فقط).
كيف أضيف مرشحة جديدة (عبر admin أو قاعدة البيانات مباشرة).
كيف أعدّل النصوص/الترجمات (ملفات messages).
ابدأ التنفيذ الآن، وبنِ المشروع كاملاً وقابلاً للتشغيل محلياً. لا تتركني في منتصف الطريق — أنجز كل صفحة وكل وظيفة.
