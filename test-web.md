
🎯 الطلب
أنت وكيل برمجة Senior Full-Stack ومهمتك بناء موقع إلكتروني متكامل وأصلي باسم test-web. الاسم تجريبي وقابل للاستبدال لاحقاً بالاسم الحقيقي، لذلك اجعل اسم الشركة والشعار والألوان وأرقام التواصل وقنوات واتساب كلها قابلة للتعديل من ملف إعدادات واحد أو متغيرات بيئة.

الهدف العام: ابنِ منصة خدمات واستقدام/توظيف منزلية حديثة تشبه وظيفياً منصات عرض المرشحين، لكن لا تنسخ أي علامة تجارية أو نصوص أو صور أو تصميم حرفيًا من أي موقع آخر. استخدم نصوصًا أصلية، صورًا تجريبية مرخصة أو placeholders، وهوية بصرية مستقلة باسم test-web.

🧱 التقنية الإلزامية
استخدم Next.js مع App Router وTypeScript.
استخدم Node.js Route Handlers داخل Next.js للـ API، ولا تنشئ Backend منفصل إلا إذا كان هناك سبب واضح.
استخدم CSS Modules أو ملف CSS منظم. لا تعتمد على Tailwind إلا إذا كان ذلك سيحسن السرعة بوضوح (إن استخدمته، اجعل الألوان tokens مسماة في config لتسهيل إعادة الاسم التجاري).
استخدم Prisma ORM مع SQLite للتطوير وPostgreSQL للإنتاج.
استخدم Zod للتحقق من مدخلات النماذج وReact Hook Form عند الحاجة.
جهّز المشروع لـ ESLint وPrettier واختبارات Playwright الأساسية.
بدون نظام تسجيل دخول للعملاء في هذه المرحلة — فقط لوحة تحكم admin بسيطة لاحقاً (اختياري).
🌐 اللغات والاتجاه
العربية هي اللغة الافتراضية، مع RTL كامل وواجهة عربية سليمة.
أضف الإنجليزية تحت /en مع LTR.
أنشئ LanguageSwitcher يحافظ على الصفحة الحالية عند تغيير اللغة قدر الإمكان.
لا تخلط كلمات إنجليزية في البيانات العربية إلا لأسماء المرشحين أو البيانات التي لم تُترجم بعد.
اجعل جميع النصوص في ملفات ترجمة (messages/ar.json وmessages/en.json) وليس داخل المكونات مباشرة — استخدم دالة t("key").
🎨 الهوية والتصميم
لوحة الألوان (قابلة للتبديل من ملف ثابت واحد):
اللون الأساسي (Primary): ذهبي/كهرماني دافئ #C8A04A (يوحي بالثقة والفخامة الخليجية)
لون مماثل (Secondary): أزرق داكن #1B3A5C
خلفية: أبيض ناصع #FFFFFF + رمادي فاتح جداً #F8FAFC
نص: رمادي قاتم #1F2937
ألوان الحالة: أخضر #16A34A (متاحة)، ذهبي #F59E0B (موثّقة)
اجعلها كلها tokens مسماة (في config أو CSS variables) لتسهيل إعادة الاسم التجاري لاحقاً.
الخطوط:
العربي: "Cairo" أو "Tajawal" (Google Fonts) مع fallback آمن
الإنجليزي: "Inter"
لا تجعل النص صغيراً.
معايير التصميم:
هوية أصلية راقية وهادئة مناسبة لخدمة عائلية.
صمم Desktop وTablet وMobile، واختبر عرض 390px و768px و1440px.
الأزرار واضحة، حالات hover/focus/disabled ظاهرة، contrast مناسب.
Header ثابت أو شبه ثابت، قائمة هاتف، Breadcrumbs، Footer، زر واتساب عائم قابل للتعطيل.
حركات لطيفة (CSS transitions): fade-in عند الظهور، عدّادات أرقام متحركة، hover على البطاقات.
🗺️ خريطة الموقع (Sitemap)
استخدم مسارات مستقلة ونظيفة (RESTful)، لا تنسخ مسارات المرجع:

الواجهة العامة:
/                       → الصفحة الرئيسية (و /en كنظير)
/candidates             → كتالوج المرشحين (فلترة/بحث/ترتيب/تصفّح)
/candidates/[slug]     → صفحة مرشح فردي (بيانات + سيرة + نموذج حجز)
/favorites              → المفضلة (محفوظات القلب)
/services               → الخدمات (قائمة + روابط لصفحات فرعية)
/services/full-time-live-in
/services/housemaids
/services/training
/services/recruitment
/categories/nanny       → صفحة فئة (تُفلتر الكتالوج)
/categories/ethiopian
/categories/filipina
/categories/ugandan
/about                  → من نحن
/contact                → تواصل معنا (نموذج + خريطة)
/blog                   → المدونة (قائمة مقالات)
/blog/[slug]           → مقال فردي
/terms                  → الشروط والأحكام
/privacy                → سياسة الخصوصية

API:
/api/candidates         → فلترة/بحث الكتالوج (JSON)
/api/candidates/[slug] → الملف العام للمرشح
/api/inquiries          → استقبال نماذج الاستفسار/الحجز
/api/favorites/toggle   → تبديل المفضلة (اختياري للمزامنة)
/api/newsletter         → النشرة البريدية (حفظ فعلي أو رسالة واضحة)

اختياري لاحقاً:
/admin                  → لوحة إدارة المرشحات
📄 مواصفات كل صفحة بالتفصيل
1) الصفحة الرئيسية (/)
الأقسام من الأعلى للأسفل:

Top bar: علم قطر + نص "مكتب استقدام مرخّص ومعتمد لخدمة جميع مناطق قطر" (يمين) + رقم الهاتف (رابط tel:) + زر تبديل اللغة (يسار).
Header/Sticky: شعار test-web (متغيّر) + قائمة تنقّل: الرئيسية / المرشحون / خدماتنا / من نحن / المفضلة / تواصل. يصبح شفافاً ثم يأخذ ظلاً عند التمرير. زر بارز "تصفح المرشحين".
Hero: عنوان أصلي (مثل "رحّب بمساعدة موثوقة تناسب منزلك في قطر") مع كلمة مميزة بلون مختلف + فقرة وصف قصيرة + زرّان:
أساسي: "تصفّح المرشحين" → /candidates
ثانوي: "اطلب عرضًا مجانيًا" → تمرير ناعم لنموذج #contact أسفل الصفحة.
شارات ثقة صغيرة: "ملفات موثّقة 100%" / "شركة مرخّصة" / "دعم مستمر" / "إجراءات واضحة".
شريط متحرك (ticker) بنوعيات الخدمة: مساعدة فلبينية / إثيوبية / أوغندية / إقامة كاملة / مربيات ورعاية مسنين / طبخ وتدبير منزلي.
بطاقات الخدمات الرئيسية: 4 بطاقات (إقامة كاملة / عاملات منزليات / تدريب / توظيف وتعيين) + روابط لصفحاتها.
إحصاءات (عدّادات متحركة عند الظهور): عدد المرشحين، عدد العائلات المخدومة، سنوات الخبرة، متوسط التقييم. قابلة للإعداد من config.
"لماذا تختار [الشركة]": 5-6 بطاقات مزايا (أيقونة + عنوان + وصف): مرخّص ومعتمد / مرشحون مدرّبون وموثّقون / إجراءات شفّافة / دعم مستمر / إقامة كاملة / جنسيات متعددة.
"مرشحون مميزون": 6 بطاقات مرشحين (تُجلب من DB) + زر "عرض جميع المرشحين +N" → /candidates.
"كيف تعمل الخدمة؟": 3 مراحل بأرقام كبيرة وأيقونات: 1) اختر مرشحك 2) راجع وأكمل المستندات 3) استقبله في منزلك.
قسم قيمة/أسعار: "تبدأ من X ر.ق / شهرياً" + مزايا (عروض شاملة / ملفات موثّقة / ضمان استبدال) + تنبيه واضح أن السعر النهائي يُؤكَّد مع الشركة + زر "اطلب عرضك المجاني" + زر "شاهد الملفات والأسعار".
شهادات عملاء تجريبية: 3 بطاقات تقييم (نجوم + اقتباس + اسم + منطقة + أحرف أولى) — موسومة بوضوح كبيانات Demo.
أدلّة للعائلات: 3 بطاقات أحدث مقالات من المدونة + روابط "اقرأ المزيد".
الأسئلة الشائعة: Accordion accessible (استخدم details/summary أو Accordion مع aria-expanded) — 5+ أسئلة، كل عنصر يفتح/يغلق.
نموذج "اطلب عرضًا مجانيًا" (id="contact"): حقول: الاسم الكامل، الهاتف/واتساب، البريد الإلكتروني، نوع الخدمة (select: مساعدة إقامة كاملة / مربية أطفال / طبخ وتدبير منزلي / رعاية مسنين)، الرسالة. زر "إرسال الطلب" → POST /api/inquiries. عند النجاح: رسالة toast خضراء + رقم طلب تجريبي + إفراغ الحقول. لا تعرض نجاحًا وهميًا قبل التأكيد.
معلومات التواصل: هاتف، إيميل، العنوان، ساعات العمل + أيقونات.
Footer: 4 أعمدة:
الشركة: الرئيسية/خدماتنا/من نحن/المدونة/تواصل/الشروط/الخصوصية
عاملاتنا حسب الجنسية: 4 روابط (categories)
ابقَ على تواصل: نموذج نشرة بريدية (بريد + زر اشترك — لا تعرض alert تجريبيًا؛ إما احفظ البريد في جدول Newsletter أو اعرض رسالة صريحة بأن الميزة غير مفعلة)
سوشيال: فيسبوك، إنستغرام، لينكدإن، تيك توك، واتساب (أيقونات)
أسفل: © السنة الحالية [اسم الشركة]. جميع الحقوق محفوظة.
2) صفحة المرشحين (/candidates)
اعرض 12 بطاقة في الطلب الأول ثم زر "عرض المزيد" (تحميل AJAX) أو pagination.
بحث نصي (placeholder: "ابحث بالاسم أو المهارة…") + debounce ~300ms.
فلاتر (chips قابلة للنقر، تفعيل/إلغاء): الجنسية (فلبينية/إثيوبية/أوغندية) + الخبرة (0-2 سنة / 3-5 سنوات / +6 سنوات) + اللغة (إنجليزية/عربية) + الدين (مسيحية/مسلمة) + التوافر (available/booked).
ترتيب (select): الموصى به / الراتب من الأقل للأعلى / من الأعلى للأقل / الأكثر خبرة / الاسم A-Z.
زر "إعادة تعيين الكل".
مبدّل عرض: شبكة (grid) / قائمة (list) — يُحفظ في URL ?view=grid|list.
على الهاتف: افتح الفلاتر داخل Drawer مع زر إغلاق.
عند عدم وجود نتائج: اعرض رسالة مفيدة + زر تواصل.
query parameters قابلة للمشاركة: q, nationality, experience, language, religion, sort, page, view.
لا تجعل تغيير الفلتر يسبب فقدان حالة اللغة أو scroll بلا سبب.
الفلاتر/البحث/الترتيب تُحدّث الشبكة فوراً عبر AJAX (JSON) بدون إعادة تحميل كاملة.
زر مساعد علوي "هل تحتاج لمساعدة؟" → نموذج #contact.
مكون CandidateCard: اعرض الصورة، شارة حالة (متاح/موثّق)، الاسم، الجنسية، الخبرة، اللغات، الراتب، زر "عرض الملف"، زر "تواصل عبر واتساب"، زر Favorite (قلب). لا تجعل الضغط على Favorite أو الرابط يفتح Modal البطاقة. الراتب بصيغة مثل 1,200 ر.ق / شهرياً.

3) صفحة المرشح الفردية (/candidates/[slug])
Breadcrumb: الرئيسية / المرشحون / [اسم المرشح].
بطاقة العنوان: اسم المرشح + رقم مرجعي + شارات "متاح حالياً" و"موثّق".
صورة كبيرة (أو فيديو إن وجد).
3 أزرار رئيسية:
تواصل عبر واتساب — رابط https://wa.me/[WHATSAPP_NUMBER]?text=... برسالة معدة مسبقًا من متغير البيئة WHATSAPP_NUMBER، تتضمن اسم المرشح والرقم المرجعي.
تحميل السيرة الذاتية — في Demo استخدم ملف PDF تجريبيًا ولا تعرض رابطًا وهميًا حساسًا.
حفظ في المفضلة (قلب) — يعمل مع localStorage في Demo (أو API للمستخدم المسجل لاحقاً)؛ النص يتبدّل لـ"محفوظ في المفضلة"؛ القلب يصبح ممتلئًا.
جدول "البيانات الشخصية والمهارية": الجنسية، الدين، الحالة الاجتماعية، عدد الأطفال، سنوات الخبرة، اللغات المتحدثة، البلدان التي عمل بها، الراتب المتوقع.
نموذج "حجز/استعلام": باسم المرشح مخفيًا (candidateId)، الحقول: الاسم، الهاتف/واتساب، البريد (اختياري)، ملاحظات. زر "إرسال طلب الاستعلام" → POST /api/inquiries. بعد الإرسال الناجح اعرض رسالة نجاح ورقم طلب، ولا تمسح البيانات قبل التأكيد.
"مرشحون مشابهون": 3 بطاقات مقترحة في أسفل الصفحة + زر "عرض المزيد من المرشحين" → /candidates.
4) صفحة الخدمات (/services) + الصفحات الفرعية
صفحة عامة تشرح: مساعدة الإقامة الكاملة / العاملات المنزليات / التدريب / التوظيف والتعيين.
لكل خدمة فرعية صفحة مستقلة: H1 + مقدمة + فوائد + طريقة العمل + FAQ + CTA (إلى الدليل أو التواصل) + روابط داخلية ذات صلة.
لا تستخدم ادعاءات قانونية أو أسعارًا نهائية غير مؤكدة.
5) صفحات الفئات (/categories/[nationality])
عنوان (H1) مثل "مساعدة فلبينية في قطر".
فقرة SEO + بطاقات مرشحين من هذه الجنسية (تُجلب من /api/candidates?nationality=X أو توجّه لـ /candidates?nationality=X).
روابط للفئات الأخرى.
6) صفحة المفضلة (/favorites)
اعرض الملفات المحفوظة.
في Demo استخدم localStorage مع مفتاح إصدار واضح.
إن لم توجد ملفات: اعرض رسالة + زرًا للعودة إلى الدليل.
عند إزالة ملف حدّث القائمة فوراً مع undo اختياري.
7) صفحة تواصل معنا (/contact)
نموذج طلب: الاسم، الهاتف، البريد، نوع الخدمة (select)، الرسالة → POST /api/inquiries.
خريطة Google Maps مدمجة (iframe) لموقع المكتب (من config).
بطاقات معلومات: هاتف، إيميل، العنوان، ساعات العمل + أيقونات سوشيال.
8) صفحة "من نحن" (/about)
نبذة المكتب + الرسالة + الهدف + الرؤية والقيم (3 مبادئ) + لماذا تثق العائلات + الخدمات المتخصصة + الالتزام بالتدريب + عملية التوظيف السلسة + FAQ.

9) المدونة (/blog + /blog/[slug])
/blog: شبكة مقالات (صورة + عنوان + مقتطف + تصنيف + تاريخ + رابط "اقرأ المزيد"). اعرض 20 مقالًا Seeded ببيانات أصلية.
/blog/[slug]: مقال كامل (H1 + كاتب + تاريخ + صورة مرخصة + محتوى + مشاركة واتساب + مقالات ذات صلة + CTA) + JSON-LD للمقال + Open Graph + canonical.
استخدم عناوين أصلية مثل: اختيار الخدمة، أول شهر، التواصل، التدريب، إدارة الروتين، فهم التكلفة — دون نسخ نصوص مواقع أخرى.
10) الشروط والخصوصية
صفحات نصية قانونية بمحتوى نموذجي قابل للتعديل لاحقاً.

⚙️ الوظائف التفاعلية (Features)
أ) كتالوج المرشحين
تخزين البيانات في جدول Candidate بالحقول: id, uid (slug-friendly, unique), slug, name, nationality, religion, marital_status, children_count, experience_years, languages (array), countries_worked (array), salary, currency, status (available/booked), verified (bool), photo_url, cv_url, skills, video_url?, created_at.
API /api/candidates يقبل: page, view, nationality, religion, language, experience, q, sort ويعيد JSON { items, total, hasMore }.
API /api/candidates/[slug] يعيد الملف العام فقط.
الفلاتر تُحدّث URL (قابلة للمشاركة) وتُسترجع عند فتح الرابط.
ب) المفضلة
بدون تسجيل دخول: خزّن في localStorage مصفوفة favorite_uids مع مفتاح إصدار واضح.
زر القلب يبدّل الحالة محلياً + يحدّث صفحة /favorites.
(اختياري لاحقاً) POST /api/favorites/toggle/[uid] للمزامنة الخادمية مع تحقق من المستخدم.
ج) النماذج وAPI
POST /api/inquiries يحفظ inquiry في قاعدة البيانات بعد Zod validation.
حقول inquiry: candidateId (اختياري)، name، phone، email (اختياري)، service، message، locale، source، createdAt، status.
التحقق server-side إلزامي حتى لو وُجد تحقق client-side. اعرض أخطاء عربية مفهومة بجانب الحقل.
newsletter: لا تعرض alert تجريبيًا؛ إما تحفظ البريد في جدول Newsletter أو تعرض رسالة صريحة بأن الميزة غير مفعلة.
أضف rate limiting وhoneypot أو CAPTCHA للنماذج في الإنتاج.
لا تسجل كلمات مرور أو بيانات بطاقة أو مستندات حساسة في logs.
د) زر التواصل العائم (Floating Actions)
ثابت أسفل اليمين: واتساب (wa.me)، اتصال (tel:)، الموقع (Google Maps) — يظهر في كل الصفحات، قابل للتعطيل من config.
هـ) i18n (تعدد اللغات)
ملفات messages/ar.json وmessages/en.json.
كل النصوص عبر t("key") — لا تثبّت أي نص مباشرة في المكونات.
LanguageSwitcher يحفظ الاختيار في cookie ويعيد توجيه لنظير الصفحة باللغة الأخرى.
و) الدفع والحجز
لا تنفذ بوابة دفع في النسخة الأولى ولا تعرض نجاح دفع وهميًا.
ضع ملاحظة واضحة أن النموذج يرسل استعلامًا وأن الرسوم وطريقة السداد تُؤكَّد مع الشركة.
أنشئ abstraction باسم PaymentProvider أو feature flag فقط للتحضير المستقبلي، بدون ربط فعلي حتى يتم تزويد مفاتيح مزود دفع معتمد.
إذا طلبت نسخة مستقبلية الدفع، أضف قبل الدفع: عرض سعر، شروط، فاتورة، حالة الطلب، صفحة نجاح، webhooks، وعدم تخزين بيانات البطاقة.
🛡️ SEO والأمان وإمكانية الوصول
SEO:
metadata فريدة لكل صفحة، canonical، sitemap.xml، robots.txt، Open Graph، Twitter Cards، وJSON-LD مناسب.
امنع duplicate routes؛ اختر صيغة واحدة للفئات ولا تنشئ نسخًا مترادفة بلا redirect/canonical.
بيانات منظمة: LocalBusiness للمكتب + Person لكل مرشح + Article للمدونة.
عناوين محسّنة (مثل "مساعدة فلبينية في قطر | [الشركة]").
hreflang (ar/en).
الأمان:
استخدم env.example ولا تضع أسرارًا في المستودع.
أضف headers أمنية أساسية.
sanitize للمحتوى.
تحقق server-side إلزامي.
لا تسجل بيانات حساسة في logs.
إمكانية الوصول (Accessibility):
semantic HTML، labels، keyboard navigation، focus states، aria-expanded للقوائم، alt مفيد للصور.
contrast مناسب.
🧩 ملف الإعدادات المركزي (إلزامي)
أنشئ src/config/site.ts يحتوي كل القيم القابلة للتبديل في مكان واحد:

export const SITE = {
  name: "test-web",            // ← يُستبدل بالاسم التجاري لاحقاً
  nameAr: "تست-ويب",            // الاسم العربي
  taglineAr: "مكتب استقدام خدم مرخّص في قطر",
  phone: "+97400000000",       // استبدل برقمك
  whatsapp: "97400000000",     // من env: WHATSAPP_NUMBER
  email: "info@test-web.com",
  addressAr: "العنوان هنا، قطر",
  workingHoursAr: "السبت–الخميس · 9ص – 9م",
  mapEmbedUrl: "https://maps.google.com/...",
  social: {
    facebook: "...", instagram: "...", linkedin: "...",
    tiktok: "...", whatsapp: "https://wa.me/97400000000",
  },
  minSalary: 1400,
  currency: "ر.ق",
  defaultLocale: "ar",
  locales: ["ar", "en"],
  floatingActionsEnabled: true,
} as const;
كل المكوّنات تستورد من هذا الملف — عند إطلاق المشروع يُغيَّر مكان واحد فقط. رقم واتساب والبريد والعنوان كمتغيرات بيئة، لا تضعها داخل مكونات كثيرة.

📦 هيكل المشروع المقترح
test-web/
├─ src/
│  ├─ app/
│  │  ├─ [locale]/
│  │  │  ├─ page.tsx                 (الرئيسية)
│  │  │  ├─ candidates/page.tsx
│  │  │  ├─ candidates/[slug]/page.tsx
│  │  │  ├─ services/page.tsx
│  │  │  ├─ services/[slug]/page.tsx
│  │  │  ├─ categories/[nationality]/page.tsx
│  │  │  ├─ about/page.tsx
│  │  │  ├─ favorites/page.tsx
│  │  │  ├─ contact/page.tsx
│  │  │  ├─ blog/page.tsx
│  │  │  ├─ blog/[slug]/page.tsx
│  │  │  ├─ terms/page.tsx
│  │  │  └─ privacy/page.tsx
│  │  ├─ api/
│  │  │  ├─ candidates/route.ts
│  │  │  ├─ candidates/[slug]/route.ts
│  │  │  ├─ inquiries/route.ts
│  │  │  ├─ favorites/toggle/route.ts
│  │  │  └─ newsletter/route.ts
│  │  ├─ layout.tsx
│  │  └─ globals.css
│  ├─ components/
│  │  ├─ layout/ (Header, Footer, TopBar, FloatingActions, Breadcrumb, MobileNav, LanguageSwitcher)
│  │  ├─ home/ (Hero, ServiceCards, Stats, WhyUs, FeaturedCandidates, Steps, Pricing, Testimonials, BlogTeaser, FAQ, ContactForm)
│  │  ├─ candidates/ (Filters, SearchBar, SortSelect, ViewToggle, CandidateCard, FilterDrawer, CandidateModal, LoadMore, NoResults, FavoriteButton, EmptyState)
│  │  ├─ candidate/ (CandidateHeader, CandidateDetails, CandidateActions, InquiryForm, RelatedCandidates)
│  │  ├─ ui/ (Button, Input, Select, Textarea, Badge, Toast, Accordion, Card, Modal, LoadingSkeleton)
│  │  └─ blog/ (BlogCard)
│  ├─ lib/
│  │  ├─ db.ts
│  │  ├─ validations.ts (Zod)
│  │  ├─ i18n.ts
│  │  ├─ whatsapp.ts
│  │  └─ utils.ts
│  ├─ hooks/
│  │  └─ useFavorites.ts
│  ├─ messages/
│  │  ├─ ar.json
│  │  └─ en.json
│  └─ config/
│     └─ site.ts
├─ prisma/
│  ├─ schema.prisma
│  └─ seed.ts
├─ public/
│  ├─ images/
│  ├─ cv/ (ملفات PDF تجريبية)
│  └─ robots.txt
├─ tests/ (Playwright)
├─ .env.example
├─ next.config.mjs
├─ tsconfig.json
├─ eslint.config.js
└─ package.json
🌱 البيانات الأولية (Seed Data)
أنشئ على الأقل:

30 مرشحًا Demo بتنوع الجنسيات (فلبينية/إثيوبية/أوغندية) والخبرة واللغات والرواتب (1200-1800) والمهارات والحالات الاجتماعية والأديان، مع صور placeholder (استخدم https://i.pravatar.cc أو placehold.co).
20 مقالًا مدونة ببيانات أصلية (عناوين مثل: اختيار الخدمة، أول شهر، التواصل، التدريب، إدارة الروتين، فهم التكلفة).
4 خدمات.
4 فئات.
3 شهادات موسومة بوضوح كـ Demo.
5 أسئلة شائعة.
كل البيانات معلّمة في الكود كـ seed وقابلة للاستبدال ببيانات حقيقية لاحقاً.

📝 تدفق المستخدم (User Flow)
يدخل المستخدم إلى الصفحة الرئيسية أو صفحة فئة.
يضغط "تصفّح المرشحين" وينتقل إلى دليل المرشحين.
يستخدم البحث أو الجنسية أو الخبرة أو اللغة أو الديانة أو الفرز.
يفتح بطاقة المرشح لرؤية الملف أو نافذة التفاصيل.
يختار حفظًا، واتساب، تحميل CV، أو إرسال استعلام.
نموذج الاستعلام يعرض رسالة نجاح ورقم طلب تجريبي بعد الحفظ في قاعدة البيانات.
لا يوجد دفع في النسخة الأولى — الرسوم وطريقة السداد تُؤكَّد مع الشركة.
✅ معايير القبول (Acceptance Criteria)
npm install ثم npm run dev يشغل المشروع بلا أخطاء.
npm run lint وnpm run typecheck ينجحان.
كل المسارات السابقة تعمل في العربية والإنجليزية.
البحث والفلاتر والفرز وLoad More وGrid/List وReset تعمل فعليًا.
فتح الملف، واتساب، Favorite، Modal، ونموذج Inquiry تعمل.
النموذج لا يرسل عند وجود حقول ناقصة ويعرض الخطأ بجانب الحقل.
اختبار Playwright يغطي: الرئيسية، الدليل، فلتر، ملف، مفضلة، نموذج، وتغيير اللغة.
لا توجد روابط ميتة أو أزرار بلا سلوك مقصود.
لا توجد نصوص منسوخة أو أسماء أو أرقام أو صور من الموقع المرجعي في المشروع.
اكتب README يشرح: التثبيت، env، قاعدة البيانات، seed، الاختبارات، وكيفية تغيير اسم test-web والهوية لاحقًا.
🚀 طريقة التنفيذ
نفّذ المشروع على مراحل، وبعد كل مرحلة شغّل lint/typecheck واختبارًا بصريًا:

إعداد Next.js والهوية.
البيانات والـ API.
المكونات المشتركة.
الدليل والملف والمفضلة.
الخدمات والمدونة والقانونية.
i18n وSEO.
الاختبارات والمراجعة النهائية.
لا تكتفِ بإنشاء صفحات شكلية؛ كل زر يجب أن يملك سلوكًا حقيقيًا أو رسالة واضحة تشرح سبب عدم توفر الوظيفة.

📌 ملاحظات نهائية
استبدل لاحقًا test-web باسم الشركة الحقيقي من ملف إعدادات واحد.
جهّز رقم واتساب والبريد والعنوان كمتغيرات بيئة، لا تضعها داخل مكونات كثيرة.
لا تستخدم صور أو نصوص الموقع المرجعي دون حقوق؛ المطلوب تشابه وظيفي لا نسخ هوية أو محتوى.
في البداية استخدم بيانات Demo ثم اربط لوحة إدارة أو قاعدة بيانات حقيقية بعد اعتماد التصميم.
إن كان المطلوب HTML/CSS/JS فقط، اطلب من الوكيل تنفيذ نفس المعمارية كنسخة static مع JSON محلي وlocalStorage، ثم يمكن نقلها إلى Next.js لاحقًا.
ابدأ التنفيذ الآن، وبنِ المشروع كاملاً وقابلاً للتشغيل محليًا. لا تتركني في منتصف الطريق — أنجز كل صفحة وكل وظيفة وكل معيار قبول.
