# 📋 البرومبت الهجين لإنشاء منصة استقدام خادمات متكاملة — مشروع "test-web"

## كيف تستخدم هذا الملف؟
انسخ كل المحتوى من بداية القسم **## 🎯 الطلب** حتى نهاية الملف، ثم ألصقه بالكامل في محادثة مع وكيل ذكاء اصطناعي برمجي (مثل OpenHands أو أي agent مهندس كود) ليُنشئ لك المنصة متكاملة خطوة بخطوة.

هذا البرومبت يجمع بين:
- **معايير هندسية صارمة** (أمان، اختبارات، إمكانية وصول، أصالة، معايير قبول قابلة للتحقق).
- **اتصال لحظي كامل** بين العميل ولوحة الإدارة بدون polling أو refresh أو reload.
- **لوحة تحكم CMS كاملة** لإدارة كل المحتوى وتتبّع العملاء.
- **تفاصيل محتوى دقيقة** لكل صفحة وأقسامها.

الاسم `test-web` هو اسم كودي مؤقت — صُمّم المشروع بحيث يُستبدل من مكان واحد فقط لاحقاً. **لا تنسخ أي علامة تجارية أو نصوص أو صور أو أرقام من أي موقع مرجعي** — المطلوب تشابه وظيفي لا نسخ هوية أو محتوى.

---

## 🔧 المتطلبات المسبقة والإعداد (اقرأ هذا أولاً)

### بيانات قاعدة البيانات (Supabase — تجريبية)
قاعدة البيانات التالية تجريبية ونظيفة بدون أي بيانات سرية للعميل. ستُستبدل بقاعدة بيانات نظيفة بعد الانتهاء من العمل. استخدمها مباشرة في `.env.local`:

```
NEXT_PUBLIC_SUPABASE_URL=https://wxknpssoebirzguwcivf.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Ind4a25wc3NvZWJpcnpndXdjaXZmIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODY0ODEzOTQsImV4cCI6MjEwMjA1NzM5NH0.HFjQnaZIEjErqMCNe6xPKFgpmk80rhy9n280MMHxlyI
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Ind4a25wc3NvZWJpcnpndXdjaXZmIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc4NjQ4MTM5NCwiZXhwIjoyMTAyMDU3Mzk0fQ.n_y_AYKWGIz8jVR9euDrh8sWgfG19Yq2dlTfGFMUIS4
```

### قيم قابلة للتغيير لاحقاً (ابدأ بقيم تجريبية)
القيم التالية ستُترك تجريبية في البداية ويُمكن تغييرها لاحقاً من لوحة التحكم:
- **اسم المشروع**: `test-web` (يُغيّر من لوحة التحكم).
- **رقم الهاتف / البريد / واتساب**: قيم تجريبية `+97400000000` / `info@test-web.com` (تُغيّر من لوحة التحكم).
- **الشعار (Logo)**: سيستخدم الوكيل placeholder نصياً — يُقدّم الشعار الحقيقي لاحقاً.
- **مزود الدفع**: صفحة كعب مؤقت — يُربط المزود لاحقاً عند التوفر.
- **الحجب الجغرافي**: يبدأ معطّلاً، يُفعّل من لوحة التحكم لاحقاً.

### ملفات البيئة المرفقة
- `.env.example` — قالب بدون قيم حقيقية (آمن للرفع على GitHub).
- `.env.local` — القيم الحقيقية (محلي فقط، مدرج في `.gitignore`).
- `.gitignore` — يمنع رفع `.env.local` وملفات حساسة أخرى.

### خطوات الإعداد المطلوبة من الوكيل
1. اقرأ `.env.local` واحمل مفاتيح Supabase.
2. شغّل SQL migrations في `supabase/migrations/` لإنشاء الجداول و RLS (استخدم `service_role` key).
3. شغّل `supabase/seed.sql` لبيانات Demo الأولية.
4. فعّل Realtime على الجداول: `workers`, `bookings`, `client_data_entries`, `page_content`, `settings`, `daily_visitors`, `blocked_clients` (من Supabase Dashboard أو SQL: `alter publication supabase_realtime add table <table>`).
5. شغّل `npm install` ثم `npm run dev`.

---

## 🎯 الطلب

أنت وكيل برمجة **Senior Full-Stack** ومهمتك بناء منصة إلكترونية متكاملة وأصلية باسم `test-web`. الاسم تجريبي وقابل للاستبدال لاحقاً بالاسم الحقيقي، لذلك اجعل اسم الشركة والشعار والألوان وأرقام التواصل وقنوات واتساب كلها قابلة للتعديل من ملف إعدادات واحد أو من لوحة تحكم المدير أو من متغيرات بيئة.

### الرؤية العامة
ابنِ منصة خدمات واستقدام/توظيف منزلية حديثة تشبه وظيفياً منصات عرض المرشحين، لكن لا تنسخ أي علامة تجارية أو نصوص أو صور أو تصميم حرفياً من أي موقع آخر. استخدم نصوصاً أصلية، صوراً تجريبية مرخصة أو placeholders، وهوية بصرية مستقلة باسم `test-web`.

### الركائز الأربع للمنصة
1. **واجهة العميل**: تصفّح العاملة → توظيف → نموذج طلب → إتمام حجز → دفع (لاحقاً).
2. **لوحة تحكم المدير (CMS)**: إدارة كاملة لكل محتوى صفحات العميل (نصوص/صور/أزرار) + إدارة العاملات + إعدادات (واتساب، حجب دول، أمان).
3. **تتبّع العملاء اللحظي**: حضور لحظي + تتبّع الصفحة الحالية + بيانات النماذج عند الإرسال + حظر.
4. **اتصال لحظي (Realtime)** بين العميل ولوحة الإدارة في الاتجاهين بدون polling أو refresh أو reload.

---

## 🧱 التقنية الإلزامية

### الإطار واللغة
- **Next.js** (App Router) مع **TypeScript**.
- Node.js Route Handlers داخل Next.js للـ API المنطقي، ولا تنشئ Backend منفصل إلا إذا كان هناك سبب واضح.
- **CSS Modules** أو ملف CSS منظم بأسماء tokens. لا تعتمد على Tailwind إلا إذا كان سيحسّن السرعة بوضوح (إن استخدمته، اجعل الألوان tokens مسماة في config).

### قاعدة البيانات والاتصال اللحظي ⭐
- **Supabase** كقاعدة بيانات + مصادقة + Realtime (PostgreSQL مُدار + طبقة Auth + طبقة Realtime فوقه).
  - استخدم عميل **Supabase JS SDK** لقراءة/كتابة البيانات وللاشتراكات اللحظية (Realtime channels).
  - استخدم **Supabase Realtime Presence** لحضور الزوار اللحظي (عدد النشطين الآن) — heartbeat + push تلقائي.
  - استخدم **Supabase Realtime Subscriptions** (postgres_changes) ليدفع أي تغيير في الجداول فوراً لكل المشتركين عبر WebSocket دائم.
  - استخدم **Supabase Auth** لتسجيل دخول العميل ولوحة الإدارة.
  - استخدم **Row Level Security (RLS)** لحماية بيانات العميل مقابل الأدمن على مستوى القاعدة.
  - لتعريف المخطط والترحيلات: استخدم Supabase Dashboard أو SQL migrations منفصلة. لا تخلط Prisma مع Supabase Realtime.

### منع الوميض (No FOUC) ⭐
- استخدم **SSR/Streaming + Suspense** لمنع Flash of Unstyled Content عند تحميل الصفحة.
- أدرج **Critical CSS inline** للأنماط فوق الطية.
- استخدم **skeleton states** أثناء تحميل البيانات بدلاً من شاشات فارغة أو وميض.
- هدِر (hydrate) المكوّنات تدريجياً مع `loading.tsx` و `error.tsx` في كل مقطع مسار.
- الهدف: لا يرى العميل وميضاً أو reload إطلاقاً لا عند التنقّل ولا عند تحديث البيانات اللحظي.

### الاتصال اللحظي — القاعدة الذهبية ⭐
- **لا polling إطلاقاً**: لا `setInterval`، لا refetch دوري، لا reload.
- كل التحديثات اللحظية تتم عبر **اشتراكات Supabase Realtime** (push-based).
- تحديث واجهة React (state update) يحدث دائماً عند وصول بيانات جديدة — وهذا مرغوب وطبيعي، لكنه لا يُرى كموميض أو reload؛ الصفحة تبقى والجزء المعني يتغيّر فقط بسلاسة.
- ما نلغيه هو: إعادة تحميل الصفحة (reload) وإعادة الجلب (refetch). ما نبقيه هو: تحديث الحالة الداخلي عند وصول push.

### قاعدة بيانات النماذج ⭐
- بيانات نماذج العميل **لا تُدفع للوحة الإدارة أثناء الكتابة** (لمنع إزعاج المدير).
- تُكتب في قاعدة البيانات **فقط عند الضغط على «حفظ» أو «إرسال»** → تُدفع فوراً للوحة عبر Realtime.
- كل إرسال جديد يُنشئ **صندوق بيانات جديد** بطابع زمني مستقل، مع الاحتفاظ بالسجل القديم.

### أدوات مساعدة
- **Zod** للتحقق من مدخلات النماذج + **React Hook Form** عند الحاجة.
- جهّز المشروع لـ **ESLint** و**Prettier** واختبارات **Playwright** الأساسية.
- **Supabase JS SDK** للعميل والخادم (مع مفاتيح منفصلة: anon public للعميل، service role للخادم فقط).

---

## 🌐 اللغات والاتجاه

- العربية هي اللغة الافتراضية، مع **RTL كامل** وواجهة عربية سليمة.
- أضف الإنجليزية تحت `/en` مع **LTR**.
- أنشئ **LanguageSwitcher** يحافظ على الصفحة الحالية عند تغيير اللغة قدر الإمكان.
- لا تخلط كلمات إنجليزية في البيانات العربية إلا لأسماء المرشحين أو البيانات التي لم تُترجم بعد.
- اجعل جميع النصوص في ملفات ترجمة (`messages/ar.json` و `messages/en.json`) وليس داخل المكونات مباشرة — استخدم دالة `t("key")`.

---

## 🎨 الهوية والتصميم

### لوحة الألوان (قابلة للتبديل من ملف ثابت واحد أو من لوحة التحكم)
- اللون الأساسي (Primary): ذهبي/كهرماني دافئ `#C8A04A` (يوحي بالثقة والفخامة الخليجية).
- لون مماثل (Secondary): أزرق داكن `#1B3A5C`.
- خلفية: أبيض ناصع `#FFFFFF` + رمادي فاتح جداً `#F8FAFC`.
- نص: رمادي قاتم `#1F2937`.
- ألوان الحالة: أخضر `#16A34A` (متاحة)، ذهبي `#F59E0B` (موثّقة).
- اجعلها كلها **tokens مسماة** (في config أو CSS variables) لتسهيل إعادة الاسم التجاري لاحقاً.

### الخطوط
- العربي: `"Cairo"` أو `"Tajawal"` (Google Fonts) مع fallback آمن.
- الإنجليزي: `"Inter"`.
- لا تجعل النص صغيراً.

### معايير التصميم
- هوية أصلية راقية وهادئة مناسبة لخدمة عائلية.
- صمم Desktop وTablet وMobile، واختبر عرض `390px` و `768px` و `1440px`.
- الأزرار واضحة، حالات hover/focus/disabled ظاهرة، contrast مناسب.
- Header ثابت أو شبه ثابت، قائمة هاتف، Breadcrumbs، Footer، زر واتساب عائم قابل للتعطيل من لوحة التحكم.
- حركات لطيفة (CSS transitions): fade-in عند الظهور، عدّادات أرقام متحركة، hover على البطاقات.

---

## 🚶 رحلة العميل ومسار الحجز ⭐

مسار موجه واضح من الدخول حتى إتمام الحجز:

```
1) تسجيل/دخول العميل            → /login أو /register
        ↓
2) تصفّح العاملة                → /candidates (بحث/فلترة/ترتيب)
        ↓ [زر: توظيف هذه العاملة]
3) ملف العاملة + الشروط          → /candidates/[slug]
   (بيانات + سيرة + شروط خاصة + سياسة استرجاع)
        ↓ [زر: متابعة الحجز]
4) نموذج طلب العاملة            → /book/[slug]
   (ملخص + الشروط الخاصة + نموذج بارز)
        ↓ [زر: إتمام الحجز]
5) مراجعة الطلب                 → /checkout/[bookingId]
   (مراجعة + رقم حجز + تأكيد الشروط)
        ↓ [زر: الانتقال للدفع]
6) صفحة الدفع                   → /payment/[bookingId]
   (كعب مؤقت الآن — "قيد الإنشاء"، تُبنى لاحقاً)
```

### قواعد المسار
- كل خطوة لها زر CTA رئيسي واضح يقود للخطوة التالية.
- لا توجد روابط ميتة أو أزرار بلا سلوك.
- صفحة الملف (3) تعرض الشروط وسياسة الاسترجاع الخاصة بهذه العاملة قبل المتابعة.
- صفحة الحجز (4) تُبرز النموذج كخطوة مستقلة بارزة (وليس مدفوناً داخل الملف).
- النموذج لا يُرسل عند وجود حقول ناقصة ويعرض الخطأ بجانب الحقل.
- بيانات النموذج تُحفظ في قاعدة البيانات فقط عند الإرسال → تُدفع فوراً للوحة الإدارة.

---

## 🗺️ خريطة الموقع (Sitemap)

استخدم مسارات مستقلة ونظيفة. **افصل صفحات العميل عن صفحات الإدارة في route groups منفصلة** لسهولة التعديل لاحقاً:
- `app/(client)/` → كل صفحات العميل.
- `app/(admin)/` → كل صفحات لوحة التحكم.

### صفحات العميل — `app/(client)/`
```
/                       → الصفحة الرئيسية (و /en كنظير)
/login                  → تسجيل دخول العميل
/register               → تسجيل عميل جديد
/account                → حساب العميل (طلباته/مفضلته)
/candidates             → كتالوج المرشحين (فلترة/بحث/ترتيب/تصفّح)
/candidates/[slug]      → صفحة مرشح فردي (بيانات + سيرة + شروط + زر توظيف)
/book/[slug]            → نموذج طلب العاملة المختارة ⭐
/checkout/[bookingId]   → مراجعة الطلب + إتمام الحجز ⭐
/payment/[bookingId]    → صفحة الدفع (كعب مؤقت) ⭐
/favorites              → المفضلة (محفوظات القلب)
/services               → الخدمات (قائمة + روابط لصفحات فرعية)
/services/full-time-live-in
/services/housemaids
/services/training
/services/recruitment
/categories/nanny       → صفحة فئة (تُفلتر الكتالوج)
/categories/ethiopian
/categories/filipina
/categories/ugandan
/about                  → من نحن
/contact                → تواصل معنا (نموذج + خريطة)
/blog                   → المدونة (قائمة مقالات)
/blog/[slug]            → مقال فردي
/terms                  → الشروط والأحكام
/privacy                → سياسة الخصوصية
/blocked                → صفحة الحظر (تُعرض للعميل المحظور)
```

### صفحات الإدارة — `app/(admin)/`
```
/admin                  → لوحة التحكم (الإحصاءات + تتبّع العملاء)
/admin/login            → تسجيل دخول المدير
/admin/settings         → الإعدادات (محتوى الصفحات/العاملات/الأمان/الجغرافيا/واتساب)
/admin/content          → إدارة محتوى كل صفحات العميل (نصوص/صور/أزرار)
/admin/workers          → إدارة العاملات (إضافة/حذف/تصنيف/توفر/مكان العرض)
/admin/workers/new      → إضافة عاملة جديدة
/admin/workers/[id]     → تعديل عاملة (بيانات + شروط + سياسة استرجاع)
/admin/bookings         → إدارة الحجوزات/الطلبات
/admin/clients          → قائمة العملاء (تتبّع لحظي + حظر)
/admin/clients/[id]     → تفاصيل عميل (صناديق بيانات ديناميكية)
/admin/security         → تغيير كلمة مرور لوحة التحكم
/admin/geo              → إعدادات الحجب الجغرافي
```

### API — `app/api/`
```
/api/auth/*             → مصادقة (يُفضّل عبر Supabase Auth مباشرة)
/api/candidates         → فلترة/بحث الكتالوج (JSON)
/api/candidates/[slug]  → الملف العام للمرشح
/api/bookings           → إنشاء/استعلام حجز ⭐
/api/bookings/[id]      → تفاصيل حجز
/api/inquiries          → استقبال نماذج الاستفسار
/api/favorites/toggle   → تبديل المفضلة (مزامنة للعميل المسجل)
/api/newsletter         → النشرة البريدية
/api/admin/content      → تحديث محتوى الصفحة (مدير فقط)
/api/admin/workers      → CRUD عاملات (مدير فقط)
/api/admin/settings     → تحديث الإعدادات (واتساب، حجب، إلخ)
/api/presence           → تسجيل حضور الزائر (للعدّادات)
```

---

## 📄 مواصفات صفحات العميل

### 1) الصفحة الرئيسية (`/`)
الأقسام من الأعلى للأسفل:
- **Top bar**: علم قطر + نص "مكتب استقدام مرخّص ومعتمد لخدمة جميع مناطق قطر" (يمين) + رقم الهاتف (رابط `tel:`) + زر تبديل اللغة (يسار).
- **Header/Sticky**: شعار `test-web` (متغيّر) + قائمة تنقّل: الرئيسية / المرشحون / خدماتنا / من نحن / المفضلة / تواصل. يصبح شفافاً ثم يأخذ ظلاً عند التمرير. زر بارز "تصفح المرشحين".
- **Hero**: عنوان أصلي (مثل "رحّب بمساعدة موثوقة تناسب منزلك في قطر") مع كلمة مميزة بلون مختلف + فقرة وصف قصيرة + زرّان:
  - أساسي: "تصفّح المرشحين" → `/candidates`.
  - ثانوي: "اطلب عرضًا مجانيًا" → تمرير ناعم لنموذج `#contact` أسفل الصفحة.
- **شارات ثقة صغيرة**: "ملفات موثّقة 100%" / "شركة مرخّصة" / "دعم مستمر" / "إجراءات واضحة".
- **شريط متحرك (ticker)** بنوعيات الخدمة: مساعدة فلبينية / إثيوبية / أوغندية / إقامة كاملة / مربيات ورعاية مسنين / طبخ وتدبير منزلي.
- **بطاقات الخدمات الرئيسية**: 4 بطاقات (إقامة كاملة / عاملات منزليات / تدريب / توظيف وتعيين) + روابط لصفحاتها.
- **إحصاءات** (عدّادات متحركة عند الظهور): عدد المرشحين، عدد العائلات المخدومة، سنوات الخبرة، متوسط التقييم. قابلة للإعداد من config أو لوحة التحكم.
- **"لماذا تختار [الشركة]"**: 5-6 بطاقات مزايا (أيقونة + عنوان + وصف).
- **"مرشحون مميزون"**: 6 بطاقات مرشحين (تُجلب من DB) + زر "عرض جميع المرشحين +N" → `/candidates`.
- **"كيف تعمل الخدمة؟"**: 3 مراحل بأرقام كبيرة وأيقونات: 1) اختر مرشحك 2) راجع وأكمل المستندات 3) استقبله في منزلك.
- **قسم قيمة/أسعار**: "تبدأ من X ر.ق / شهرياً" + مزايا + تنبيه واضح أن السعر النهائي يُؤكَّد مع الشركة + زر "اطلب عرضك المجاني" + زر "شاهد الملفات والأسعار".
- **شهادات عملاء تجريبية**: 3 بطاقات تقييم — موسومة بوضوح كبيانات Demo.
- **أدلّة للعائلات**: 3 بطاقات أحدث مقالات من المدونة + روابط "اقرأ المزيد".
- **الأسئلة الشائعة**: Accordion accessible (`details/summary` أو Accordion مع `aria-expanded`) — 5+ أسئلة.
- **نموذج "اطلب عرضًا مجانيًا"** (`id="contact"`): حقول: الاسم الكامل، الهاتف/واتساب، البريد الإلكتروني، نوع الخدمة، الرسالة. زر "إرسال الطلب" → `POST /api/inquiries`. عند النجاح: رسالة toast خضراء + رقم طلب تجريبي + إفراغ الحقول.
- **معلومات التواصل**: هاتف، إيميل، العنوان، ساعات العمل + أيقونات.
- **Footer**: 4 أعمدة (الشركة / عاملاتنا حسب الجنسية / نشرة بريدية / سوشيال) + © السنة الحالية [اسم الشركة].

### 2) صفحة المرشحين (`/candidates`)
- اعرض 12 بطاقة في الطلب الأول ثم زر "عرض المزيد" (تحميل لحظي) أو pagination.
- بحث نصي (placeholder: "ابحث بالاسم أو المهارة…") + debounce ~300ms.
- فلاتر (chips قابلة للنقر، تفعيل/إلغاء): الجنسية + الخبرة + اللغة + الدين + التوافر.
- ترتيب (select): الموصى به / الراتب من الأقل للأعلى / من الأعلى للأقل / الأكثر خبرة / الاسم A-Z.
- زر "إعادة تعيين الكل".
- مبدّل عرض: شبكة (grid) / قائمة (list) — يُحفظ في URL `?view=grid|list`.
- على الهاتف: افتح الفلاتر داخل Drawer مع زر إغلاق.
- عند عدم وجود نتائج: اعرض رسالة مفيدة + زر تواصل.
- query parameters قابلة للمشاركة: `q, nationality, experience, language, religion, sort, page, view`.
- لا تجعل تغيير الفلتر يسبب فقدان حالة اللغة أو scroll بلا سبب.
- الفلاتر/البحث/الترتيب تُحدّث الشبكة فوراً عبر AJAX (JSON) بدون إعادة تحميل كاملة.
- زر مساعد علوي "هل تحتاج لمساعدة؟" → نموذج `#contact`.
- **مكون CandidateCard**: الصورة، شارة حالة (متاح/موثّق)، الاسم، الجنسية، الخبرة، اللغات، الراتب، زر "عرض الملف"، زر "تواصل عبر واتساب"، زر "توظيف" (يوجّه لصفحة الحجز) ⭐، زر Favorite (قلب). الراتب بصيغة مثل `1,200 ر.ق / شهرياً`.

### 3) صفحة المرشح الفردية (`/candidates/[slug]`)
- Breadcrumb: الرئيسية / المرشحون / [اسم المرشح].
- بطاقة العنوان: اسم المرشح + رقم مرجعي + شارات "متاح حالياً" و"موثّق".
- صورة كبيرة (أو فيديو إن وجد).
- **3 أزرار رئيسية**:
  - **توظيف هذه العاملة** ⭐ → يوجّه لـ `/book/[slug]` (بدء مسار الحجز).
  - تواصل عبر واتساب — رابط `https://wa.me/[WHATSAPP_NUMBER]?text=...` برسالة معدة مسبقًا.
  - حفظ في المفضلة (قلب) — يعمل مع Supabase للعميل المسجل؛ النص يتبدّل لـ"محفوظ في المفضلة".
- جدول "البيانات الشخصية والمهارية": الجنسية، الدين، الحالة الاجتماعية، عدد الأطفال، سنوات الخبرة، اللغات المتحدثة، البلدان التي عمل بها، الراتب المتوقع.
- **قسم الشروط وسياسة الاسترجاع** ⭐: يعرض الشروط الخاصة بهذه العاملة + سياسة الاسترجاع الخاصة بها (من DB). إن لم تُحدّد، يعرض السياسة العامة من config.
- "مرشحون مشابهون": 3 بطاقات مقترحة في أسفل الصفحة.

### 4) صفحة نموذج طلب العاملة (`/book/[slug]`) ⭐
- تعرض بيانات العاملة المختارة (صورة + اسم + رقم مرجعي + الراتب).
- تعرض **الشروط وسياسة الاسترجاع** الخاصة بالعاملة بوضوح قبل النموذج.
- **نموذج بارز** (ليس مدفوناً): الاسم الكامل، الهاتف/واتساب، البريد الإلكتروني، ملاحظات، حقل مخفي `candidateId`.
- زر "إتمام الحجز" → `POST /api/bookings` → ينشئ حجز برقم مرجعي → يوجّه لـ `/checkout/[bookingId]`.
- عند النجاح: رسالة نجاح + رقم حجز. لا تمسح البيانات قبل التأكيد.
- التحقق server-side إلزامي (Zod) + عرض أخطاء عربية مفهومة بجانب الحقل.

### 5) صفحة مراجعة الطلب (`/checkout/[bookingId]`) ⭐
- تعرض ملخص الحجز: بيانات العاملة + بيانات العميل + الشروط + رقم الحجز.
- خانة تأكيد الشروط (checkbox إلزامي).
- زر "الانتقال للدفع" → `/payment/[bookingId]`.

### 6) صفحة الدفع (`/payment/[bookingId]`) ⭐
- **كعب مؤقت الآن**: تعرض رسالة واضحة "خدمة الدفع قيد الإنشاء — سيتم التواصل معك لتأكيد الرسوم وطريقة السداد".
- تعرض رقم الحجز وملخصه.
- لا تعرض نجاح دفع وهمياً.
- abstraction باسم `PaymentProvider` جاهز للربط لاحقاً عند تزويد مفاتيح مزود دفع معتمد.

### 7) تسجيل الدخول/التسجيل (`/login`, `/register`)
- بريد + كلمة مرور (عبر Supabase Auth).
- خيار مستقبلي: OTP عبر واتساب/إيميل.
- بعد الدخول: توجيه لحساب العميل (`/account`).

### 8) حساب العميل (`/account`)
- عرض طلبات/حجوزات العميل وحالتها.
- عرض المفضلة (مزامنة مع الخادم للعميل المسجل).
- تعديل بيانات الحساب الأساسية.

### 9) صفحات الخدمات (`/services`) + الفرعية
- صفحة عامة تشرح: مساعدة الإقامة الكاملة / العاملات المنزليات / التدريب / التوظيف والتعيين.
- لكل خدمة فرعية صفحة مستقلة: H1 + مقدمة + فوائد + طريقة العمل + FAQ + CTA + روابط داخلية ذات صلة.
- لا تستخدم ادعاءات قانونية أو أسعارًا نهائية غير مؤكدة.

### 10) صفحات الفئات (`/categories/[nationality]`)
- عنوان (H1) مثل "مساعدة فلبينية في قطر".
- فقرة SEO + بطاقات مرشحين من هذه الجنسية.
- روابط للفئات الأخرى.

### 11) صفحة المفضلة (`/favorites`)
- اعرض الملفات المحفوظة (للعميل المسجل: من Supabase؛ للزائر: localStorage مؤقت).
- إن لم توجد ملفات: اعرض رسالة + زرًا للعودة إلى الدليل.

### 12) صفحة الحظر (`/blocked`) ⭐
- تُعرض للعميل المحظور (عبر middleware يتحقق IP + بصمة الجهاز).
- رسالة واضحة "تم تقييد وصولك إلى هذا الموقع" بدون كشف سبب تقني.

### 13) باقي الصفحات
- `/about`, `/contact` (نموذج + خريطة), `/blog`, `/blog/[slug]`, `/terms`, `/privacy` — بمحتوى أصلي قابل للإدارة من لوحة التحكم.

---

## 🛠️ لوحة التحكم الموسّعة ⭐

لوحة تحكم CMS كاملة تدير كل شيء وينعكس التغيير على العميل خلال أقل من 1 ثانية (عبر Supabase Realtime).

### شريط أعلى الصفحة (Desktop) — إحصاءات لحظية
كل القيم تُحدّث لحظياً عبر Realtime بدون refresh:
1. **عدد الزوار النشطين الآن** — حضور لحظي (Supabase Realtime Presence)، <1 ثانية.
2. **العدد الإجمالي لزوار اليوم** — كل عميل يُسجَّل مرة واحدة فقط في اليوم (وليس عند كل زيارة) — جدول `daily_visitors` بمفتاح (التاريخ + معرّف العميل).
3. **العدد الإجمالي لكامل الزيارات منذ بدء إطلاق الخادم** — عدّاد تراكمي يبدأ من أول تشغيل.
4. **عدد التسجيلات في النموذج** — عدد إرسالات النماذج (inquiries + bookings).
5. **عدد عمليات الدفع** — placeholder، يُفعّل عند توفر الدفع.

### زر الإعدادات → نافذة شاملة
يفتح نافذة يمكن من خلالها الانتقال إلى:
- **محتوى الصفحات**: تعديل كل نصوص/صور/أزرار صفحات العميل. كل التعديلات تنعكس على العميل خلال <1 ثانية (Realtime).
- **العاملات الموجودات**: قائمة بكل العاملات + تصنيفها (بالساعة/اليوم/الشهر/السنة) + حالة التوفر (متوفر/غير متوفر).
- **إدارة عاملة**: حذف عاملة، تغيير حالة التوفر، تعديل الشروط وسياسة الاسترجاع الخاصة بها.
- **إضافة عاملة**: نموذج إضافة + اختيار **مكان عرض العاملة** (أي صفحة/قسم تظهر فيه).
- **تبويب الأمان**: تغيير كلمة مرور لوحة التحكم.
- **الحجب الجغرافي**: حجب الموقع عن دول محددة، أو السماح لدولة واحدة فقط (حسب اختيار المدير).
- **إعدادات واتساب**: تغيير رقم واتساب من المدير + تفعيل/تعطيل زر واتساب. **عند التعطيل يُخفى الزر تماماً عن عين العميل لا يراه أبداً** (شرط عبر config + RLS).

### إدارة محتوى صفحات العميل (CMS كامل) ⭐
- **كل صفحات العميل مُدارة بالكامل**: جميع النصوص، جميع الصور، جميع الأزرار، وكل المحتوى داخل كل صفحة تحت إدارة المدير.
- يُخزَّن المحتوى في جدول `page_content` (مفتاح: الصفحة + المقطع + اللغة).
- يتمكن المدير من تغيير أي شيء بسرعة وينعكس على العميل خلال <1 ثانية (Realtime invalidation + push).
- العميل يقرأ المحتوى من نفس الجدول ويتحدّث لحظياً عند أي تغيير — لا reload.

---

## 👥 نظام تتبّع العملاء والحظر ⭐

### تخطيط لوحة تتبّع العملاء
- **شريط جانبي على يمين الشاشة**: يعرض قائمة أسماء العملاء.
  - أزرار: تحديد عميل / تحديد الكل / حذف / أرشفة / إلغاء تحديد.
- **تبويب لكل عميل**:
  - قبل اسم العميل: **SVG شكل شخص بدائرة**.
  - قبل الـ SVG: **مربع تحديد** (checkbox) لتحديد/إلغاء تحديد العميل.
  - اسم العميل.
  - أسفل الاسم: **اسم الصفحة الحالية للعميل بالوقت الفعلي** — يُجلب خلال <1 ثانية (Realtime).
  - على يمين التبويب: **آخر نشاط عام للعميل** — "منذ X ثانية/دقيقة".

### تفاصيل العميل (عند النقر على اسمه)
- النقر على اسم العميل يفتح تفاصيله على **جهة اليسار**.
- يُعرض: **دولة العميل** + **الصفحة التي يزورها حالياً**.
- **صناديق بيانات** (تُحمل بالبيانات):
  - صندوق المعلومات الأساسية.
  - صندوق بطاقة الدفع.
  - صندوق رمز التحقق.
  - **قيم هذه الصناديق وهمية حالياً** — سنعدّل شكلها/محتواها لاحقاً.
- **كل صندوق يعرض وقتاً منفصلاً** (طابع زمني) لنعرف متى تم إدخال البيانات لكل عملية.
- **عند إدخال العميل بيانات عدة مرات**: يُنشأ **صندوق جديد ديناميكياً** بداخل تفاصيل العميل (لحفظ السجل — لا نفقد بيانات قديمة أو جديدة).
- بيانات النماذج تظهر هنا **فقط عند الإرسال** (ليس أثناء الكتابة).

### الحظر
- **حظر العميل**: عند الحظر يُمنع العميل من التنقّل في صفحات الموقع نهائياً.
- مستوى الحظر مزدوج: على مستوى **شبكة الإنترنت (IP)** + **المعرّف الفريد للعميل** (بصمة الجهاز).
- عند الحظر: middleware يتحقق من IP + بصمة الجهاز في كل طلب → يوجّه لـ `/blocked`.
- **إلغاء الحظر**: يُلغى الحظر عن العميل وعن الإنترنت الخاص به (IP).
- البصمة: استخدم معرّفاً فريداً في `localStorage` + خصائص الجهاز (FingerprintJS أو معرّف مركّب).

---

## 📋 الشروط وسياسة الاسترجاع ⭐

- **لكل عاملة شروطها الخاصة**: حقل `terms` و `returnPolicy` على جدول `workers`.
- **سياسة عامة احتياطية** في ملف الإعدادات (`site.ts` → `returnPolicy`) تُستخدم حين تترك شروط العاملة فارغة.
- الشروط تُعرض في:
  - صفحة ملف العاملة (`/candidates/[slug]`).
  - صفحة نموذج الحجز (`/book/[slug]`).
  - صفحة مراجعة الطلب (`/checkout/[bookingId]`).
- الشروط **قابلة للتعديل من لوحة التحكم** لكل عاملة على حدة.
- اختيار العميل للعاملة → يُحمل نموذج الحجز + شروط تلك العاملة تحديداً.

---

## 💳 الدفع ⭐ (مُحدّث)

- **لا تنفذ بوابة دفع في النسخة الأولى** ولا تعرض نجاح دفع وهمياً.
- صفحة `/payment/[bookingId]` **كعب مؤقت** يوضح أن الخدمة قيد الإنشاء.
- أنشئ abstraction باسم `PaymentProvider` أو feature flag فقط للتحضير المستقبلي، بدون ربط فعلي حتى يتم تزويد مفاتيح مزود دفع معتمد.
- إذا طلبت نسخة مستقبلية الدفع، أضف قبل الدفع: عرض سعر، شروط، فاتورة، حالة الطلب، صفحة نجاح، webhooks، وعدم تخزين بيانات البطاقة.
- **عداد عمليات الدفع** في شريط لوحة التحكم جاهز (placeholder) ويُفعّل عند توفر الدفع.

---

## 🛡️ SEO والأمان وإمكانية الوصول

### SEO
- metadata فريدة لكل صفحة، canonical، `sitemap.xml`، `robots.txt`، Open Graph، Twitter Cards، وJSON-LD مناسب.
- امنع duplicate routes؛ اختر صيغة واحدة للفئات ولا تنشئ نسخًا مترادفة بلا redirect/canonical.
- بيانات منظمة: `LocalBusiness` للمكتب + `Person` لكل مرشح + `Article` للمدونة.
- عناوين محسّنة (مثل "مساعدة فلبينية في قطر | [الشركة]").
- `hreflang` (ar/en).

### الأمان
- استخدم `.env.example` ولا تضع أسرارًا في المستودع.
- مفاتيح Supabase: `anon` للعميل، `service_role` للخادم فقط (لا تكشفها للعميل).
- أضف headers أمنية أساسية.
- sanitize للمحتوى.
- تحقق server-side إلزامي (Zod).
- لا تسجل بيانات حساسة في logs.
- **Row Level Security (RLS)** على كل الجداول الحساسة.
- حماية صفحات الحجز/الحساب بالمصادقة (middleware + Supabase Auth).
- حماية صفحات الإدارة بصلاحية admin.
- rate limiting و honeypot أو CAPTCHA للنماذج في الإنتاج.

### إمكانية الوصول (Accessibility)
- semantic HTML، labels، keyboard navigation، focus states، `aria-expanded` للقوائم، `alt` مفيد للصور.
- contrast مناسب.

---

## ⚙️ ملف الإعدادات المركزي (إلزامي)

أنشئ `src/config/site.ts` يحتوي كل القيم القابلة للتبديل في مكان واحد:

```ts
export const SITE = {
  name: "test-web",
  nameAr: "تست-ويب",
  taglineAr: "مكتب استقدام خدم مرخّص في قطر",
  phone: process.env.PHONE || "+97400000000",
  whatsapp: process.env.WHATSAPP_NUMBER || "97400000000",
  whatsappEnabled: true, // يُتحكم بها أيضاً من لوحة التحكم
  email: process.env.SITE_EMAIL || "info@test-web.com",
  addressAr: "العنوان هنا، قطر",
  workingHoursAr: "السبت–الخميس · 9ص – 9م",
  mapEmbedUrl: "https://maps.google.com/...",
  social: {
    facebook: "...", instagram: "...", linkedin: "...",
    tiktok: "...", whatsapp: "https://wa.me/97400000000",
  },
  minSalary: 1400,
  currency: "ر.ق",
  defaultLocale: "ar",
  locales: ["ar", "en"],
  floatingActionsEnabled: true,
  returnPolicy: "السياسة العامة للاسترجاع/الاستبدال...", // احتياطية
  geoBlocking: {
    enabled: false,
    mode: "allowlist" | "blocklist", // allowlist = دولة واحدة فقط
    countries: [],
  },
  payment: {
    enabled: false, // placeholder حتى التوفر
    provider: "stub",
  },
} as const;
```
كل المكوّنات تستورد من هذا الملف — عند الإطلاق يُغيَّر مكان واحد فقط. القيم القابلة للتغيير من لوحة التحكم (واتساب، حجب، محتوى) تُقرأ من Supabase وتتجاوز config عند وجودها. رقم واتساب والبريد والعنوان كمتغيرات بيئة، لا تضعها داخل مكونات كثيرة.

---

## 📦 هيكل المشروع المقترح

```
test-web/
├─ src/
│  ├─ app/
│  │  ├─ (client)/              ← كل صفحات العميل ⭐
│  │  │  ├─ [locale]/
│  │  │  │  ├─ page.tsx
│  │  │  │  ├─ candidates/page.tsx
│  │  │  │  ├─ candidates/[slug]/page.tsx
│  │  │  │  ├─ book/[slug]/page.tsx          ⭐
│  │  │  │  ├─ checkout/[bookingId]/page.tsx ⭐
│  │  │  │  ├─ payment/[bookingId]/page.tsx  ⭐
│  │  │  │  ├─ services/page.tsx
│  │  │  │  ├─ services/[slug]/page.tsx
│  │  │  │  ├─ categories/[nationality]/page.tsx
│  │  │  │  ├─ about/page.tsx
│  │  │  │  ├─ favorites/page.tsx
│  │  │  │  ├─ contact/page.tsx
│  │  │  │  ├─ blog/page.tsx
│  │  │  │  ├─ blog/[slug]/page.tsx
│  │  │  │  ├─ terms/page.tsx
│  │  │  │  ├─ privacy/page.tsx
│  │  │  │  ├─ account/page.tsx
│  │  │  │  ├─ login/page.tsx
│  │  │  │  ├─ register/page.tsx
│  │  │  │  └─ blocked/page.tsx
│  │  ├─ (admin)/               ← كل صفحات الإدارة ⭐
│  │  │  ├─ admin/page.tsx
│  │  │  ├─ admin/login/page.tsx
│  │  │  ├─ admin/settings/page.tsx
│  │  │  ├─ admin/content/page.tsx
│  │  │  ├─ admin/workers/page.tsx
│  │  │  ├─ admin/workers/new/page.tsx
│  │  │  ├─ admin/workers/[id]/page.tsx
│  │  │  ├─ admin/bookings/page.tsx
│  │  │  ├─ admin/clients/page.tsx
│  │  │  ├─ admin/clients/[id]/page.tsx
│  │  │  ├─ admin/security/page.tsx
│  │  │  └─ admin/geo/page.tsx
│  │  ├─ api/
│  │  │  ├─ candidates/route.ts
│  │  │  ├─ candidates/[slug]/route.ts
│  │  │  ├─ bookings/route.ts
│  │  │  ├─ bookings/[id]/route.ts
│  │  │  ├─ inquiries/route.ts
│  │  │  ├─ favorites/toggle/route.ts
│  │  │  ├─ newsletter/route.ts
│  │  │  ├─ presence/route.ts
│  │  │  ├─ admin/content/route.ts
│  │  │  ├─ admin/workers/route.ts
│  │  │  └─ admin/settings/route.ts
│  │  ├─ layout.tsx
│  │  └─ globals.css
│  ├─ components/
│  │  ├─ client/   (Header, Footer, TopBar, FloatingActions, Breadcrumb, MobileNav, LanguageSwitcher, Hero, ServiceCards, Stats, CandidateCard, BookingForm, CheckoutSummary, PaymentStub, ClientAuth)
│  │  ├─ admin/    (AdminHeader, StatsBar, SettingsPanel, WorkerManager, ClientTracker, ClientList, ClientDetail, DataBox, ContentEditor, GeoBlocker, WhatsAppToggle)
│  │  ├─ ui/       (Button, Input, Select, Textarea, Badge, Toast, Accordion, Card, Modal, LoadingSkeleton, Checkbox)
│  │  └─ blog/     (BlogCard)
│  ├─ lib/
│  │  ├─ supabase/      (client.ts, server.ts, middleware.ts, types.ts)
│  │  ├─ realtime.ts    (اشتراكات Realtime الموحّدة)
│  │  ├─ presence.ts    (حضور الزوار اللحظي)
│  │  ├─ fingerprint.ts (بصمة الجهاز + الحظر)
│  │  ├─ validations.ts (Zod)
│  │  ├─ i18n.ts
│  │  ├─ whatsapp.ts
│  │  └─ utils.ts
│  ├─ hooks/
│  │  ├─ useFavorites.ts
│  │  ├─ usePresence.ts
│  │  ├─ useRealtime.ts
│  │  └─ usePageContent.ts
│  ├─ messages/
│  │  ├─ ar.json
│  │  └─ en.json
│  └─ config/
│     └─ site.ts
├─ supabase/
│  ├─ migrations/        (SQL migrations للمخطط)
│  ├─ seed.sql
│  └─ rls.sql            (سياسات Row Level Security)
├─ public/
│  ├─ images/
│  ├─ cv/ (ملفات PDF تجريبية)
│  └─ robots.txt
├─ tests/ (Playwright)
├─ .env.example
├─ next.config.mjs
├─ tsconfig.json
├─ eslint.config.js
└─ package.json
```

---

## 🗄️ المخطط المقترح (Supabase / PostgreSQL)

```sql
-- العاملات
workers (
  id uuid pk, slug text unique, full_name text, nationality text,
  experience_years int, languages text[], religion text,
  marital_status text, children_count int, expected_salary int,
  skills text[], photo_url text, cv_url text, video_url text,
  availability text,  -- available | booked
  placement text,     -- أين تُعرض (صفحة/قسم)
  terms text,         -- الشروط الخاصة ⭐
  return_policy text, -- سياسة الاسترجاع الخاصة ⭐
  employment_type text, -- hourly | daily | monthly | yearly ⭐
  created_at timestamptz
)

-- العملاء
clients (
  id uuid pk, email text unique, phone text, name text,
  country text, fingerprint text unique, ip text,
  is_blocked bool default false, created_at timestamptz
)

-- الحجوزات/الطلبات
bookings (
  id uuid pk, booking_ref text unique, client_id uuid fk,
  worker_id uuid fk, status text, notes text,
  terms_snapshot text, return_policy_snapshot text,
  created_at timestamptz
)

-- بيانات النماذج (صناديق ديناميكية)
client_data_entries (
  id uuid pk, client_id uuid fk, type text,  -- basic | payment | verification
  payload jsonb, created_at timestamptz
)  -- كل إرسال = صف جديد (صندوق جديد) ⭐

-- حضور الزوار
daily_visitors (
  date date, client_id uuid, fingerprint text,
  unique(date, client_id)
)

-- محتوى الصفحات (CMS)
page_content (
  id uuid pk, page text, section text, locale text,
  content jsonb, updated_at timestamptz
)

-- الإعدادات (يديرها المدير)
settings (
  key text pk, value jsonb, updated_at timestamptz
)

-- النشرة البريدية
newsletter (id uuid pk, email text unique, created_at timestamptz)

-- حظر IP/بصمة
blocked_clients (
  id uuid pk, fingerprint text, ip text, blocked_at timestamptz
)
```

---

## 🌱 البيانات الأولية (Seed Data)

أنشئ على الأقل:
- **30 عاملة Demo** بتنوع الجنسيات (فلبينية/إثيوبية/أوغندية) والخبرة واللغات والرواتب (1200-1800) والمهارات والحالات الاجتماعية والأديان، مع **شروط وسياسة استرجاع خاصة بكل عاملة**، وتصنيف employment_type (بالساعة/اليوم/الشهر/السنة)، وصور placeholder (استخدم `https://i.pravatar.cc` أو `placehold.co`).
- **20 مقالاً** للمدونة ببيانات أصلية.
- **4 خدمات**، **4 فئات**، **3 شهادات** موسومة بـ Demo، **5 أسئلة شائعة**.
- **محتوى CMS افتراضي** لكل صفحات العميل (نصوص/صور/أزرار).
- كل البيانات معلّمة في الكود كـ seed وقابلة للاستبدال ببيانات حقيقية لاحقاً.

---

## ✅ معايير القبول (Acceptance Criteria)

- `npm install` ثم `npm run dev` يشغل المشروع بلا أخطاء.
- `npm run lint` و `npm run typecheck` ينجحان.
- كل المسارات تعمل في العربية والإنجليزية.
- **مسار الحجز الكامل يعمل**: تسجيل دخول → تصفّح → ملف عاملة (شروط) → نموذج حجز → مراجعة → صفحة دفع (كعب).
- البحث والفلاتر والفرز و Load More و Grid/List و Reset تعمل فعلياً.
- فتح الملف، واتساب، Favorite، ونموذج Inquiry تعمل.
- النموذج لا يرسل عند وجود حقول ناقصة ويعرض الخطأ بجانب الحقل.
- **الاتصال اللحظي يعمل**:
  - عدد الزوار النشطين يُحدّث لحظياً في لوحة الإدارة.
  - تنقّل العميل بين الصفحات يظهر لحظياً في قائمة العملاء (<1ث).
  - تعديل المدير لأي محتوى ينعكس على العميل خلال <1ث بدون reload.
  - بيانات النموذج تظهر في لوحة الإدارة فقط عند الإرسال.
- **لوحة التحكم تعمل**: إضافة/حذف/تعديل عاملة، تغيير التوفر، تعديل شروط عاملة، تغيير رقم واتساب، تعطيل زر واتساب (يختفي عن العميل)، حجب دول.
- **الحظر يعمل**: حظر عميل (IP + بصمة) → يُمنع من التنقّل → صفحة `/blocked`. إلغاء الحظر يعيد الوصول.
- **صناديق بيانات العميل**: تنشأ ديناميكياً عند كل إرسال، كل واحد بطابع زمني مستقل.
- اختبار Playwright يغطي: الرئيسية، الدليل، فلتر، ملف، حجز، مراجعة، مفضلة، نموذج، تغيير اللغة، تسجيل دخول، وحظر.
- لا توجد روابط ميتة أو أزرار بلا سلوك مقصود.
- لا توجد نصوص منسوخة أو أسماء أو أرقام أو صور من الموقع المرجعي في المشروع.
- اكتب README يشرح: التثبيت، env، Supabase setup، seed، الاختبارات، وكيفية تغيير اسم `test-web` والهوية لاحقاً.

---

## 🚀 طريقة التنفيذ

نفّذ المشروع على مراحل، وبعد كل مرحلة شغّل lint/typecheck واختبارًا بصريًا:
1. إعداد Next.js + Supabase + الهوية + المخطط.
2. المصادقة (عميل + أدمن) + RLS.
3. الاتصال اللحظي (Realtime + Presence) + منع الوميض.
4. المكونات المشتركة + CMS المحتوى.
5. الدليل والملف والمفضلة + مسار الحجز (حجز → مراجعة → دفع كعب).
6. لوحة التحكم (إحصاءات + عاملات + إعدادات + واتساب + جغرافيا).
7. تتبّع العملاء (قائمة + تفاصيل + صناديق ديناميكية + حظر).
8. الخدمات والمدونة والقانونية.
9. i18n و SEO.
10. الاختبارات والمراجعة النهائية.

لا تكتفِ بإنشاء صفحات شكلية؛ كل زر يجب أن يملك سلوكًا حقيقيًا أو رسالة واضحة تشرح سبب عدم توفر الوظيفة.

---

## 📌 ملاحظات نهائية

- استبدل لاحقًا `test-web` باسم الشركة الحقيقي من ملف إعدادات واحد.
- جهّز رقم واتساب والبريد والعنوان كمتغيرات بيئة، لا تضعها داخل مكونات كثيرة.
- لا تستخدم صور أو نصوص الموقع المرجعي دون حقوق؛ المطلوب تشابه وظيفي لا نسخ هوية أو محتوى.
- في البداية استخدم بيانات Demo ثم اربط لوحة إدارة أو قاعدة بيانات حقيقية بعد اعتماد التصميم.
- **Supabase** يوفر قاعدة البيانات + Auth + Realtime في طبقة واحدة — لا تبنِ خادم WebSocket منفصل.
- **لا polling إطلاقاً** — كل التحديثات اللحظية عبر اشتراكات Supabase Realtime.
- **منع الوميض** عبر SSR/Streaming + skeleton + Critical CSS.
- ابدأ التنفيذ الآن، وبنِ المشروع كاملاً وقابلاً للتشغيل محلياً. لا تتركني في منتصف الطريق — أنجز كل صفحة وكل وظيفة وكل معيار قبول.
